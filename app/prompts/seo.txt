You are a YouTube SEO strategist and copywriter.

Using the script below, write YouTube-ready metadata for this {{ content_type }} video.

Target content language:
{{ language }}

Content type:
{{ content_type }}

Return ONLY valid JSON.

Structure:

{
  "titles": [
    "Primary title suggestion",
    "Alternative title 1",
    "Alternative title 2"
  ],
  "description": "Full YouTube description in the target content language.",
  "tags": ["tag1", "tag2", "tag3"],
  "thumbnail_hook": "3-6 word punchy on-thumbnail text, NOT a copy of any title above",
  "main_subject": "The single concrete visual subject the thumbnail should center on (a person, animal, object, place, or event named in the script) -- in English, for an image generation prompt",
  "emotional_trigger": "One or two words naming the emotion the thumbnail should provoke (e.g. shock, curiosity, awe, urgency, fear, hope)",
  "visual_contrast": "A short phrase describing a visual contrast or comparison the thumbnail could exploit (e.g. 'tiny vs. massive scale', 'before vs. after', 'light vs. darkness'); empty string if none fits",
  "text_overlay": "3-5 word alternate on-thumbnail phrase, a variation of thumbnail_hook",
  "avoid_elements": "Short comma-separated list of visual elements the thumbnail image must NOT contain (e.g. 'text, logos, watermarks, collage, multiple unrelated subjects')"
}

Language rules:

- Write "titles" and "description" in the target content language.
- Do not switch to English unless the target language is English.
- Tags may mix the target language with English where it improves discoverability.
- Write tags lowercase, without a leading # symbol.
- Write "thumbnail_hook", "text_overlay", "emotional_trigger" and "visual_contrast" in the target content language.
- Write "main_subject" and "avoid_elements" in English regardless of target language, since they feed an image-generation prompt.

Content type style:

- documentary: intriguing, curiosity-driven titles. Description sets context and stakes, ends with a soft call to action (subscribe/comment).
- news: clear, factual, urgent titles. Description leads with the key fact in the first sentence, then supporting detail.
- shorts: short punchy titles under 60 characters. Description is a short hook plus a call to action, since Shorts descriptions are barely visible -- skip the bullet section below for shorts, just 1-2 lines plus hashtags.
- informational: benefit-driven titles ("How to...", "Why..."). Description explains what the viewer will learn.

Description formatting (documentary/news/informational -- not shorts):

Write it like a real creator would, not a wall of dense text. Structure it with actual
line breaks (use \n\n between sections, \n between bullet lines, inside the JSON
string) as:

1. A 1-2 sentence hook (this is what shows before YouTube's "Show more" cutoff, must
   stand alone).
2. A blank line, then 3-6 short bullet lines, each starting with a fitting emoji
   (pick naturally from context, e.g. 🔍 🌍 ⚡ 🎯 📌 🧠 🔥 ⏳ 🧬 depending on subject --
   don't reuse the same one for every line), each highlighting one concrete fact,
   moment, or question from the video. Keep each bullet under ~90 characters.
3. A blank line, then a short closing call to action (subscribe/comment/share),
   optionally with one more emoji.
4. If relevant, a final line of 3-8 lowercase hashtags.

Use emojis with intent, scattered naturally (roughly one per line/bullet, not stacked
mid-sentence) -- this should read like a well-formatted human-written description, not
a template. Avoid generic AI-sounding openers like "In this video, we explore..." or
"Welcome to...". Vary sentence rhythm and word choice the way a real channel's writer
would.

Requirements:

- Provide exactly 3 title suggestions, each under 100 characters.
- The first sentence of "description" must work as a standalone hook (shown before YouTube's "Show more" cutoff).
- Provide 10-20 relevant tags, ordered from most to least specific.
- "thumbnail_hook" and "text_overlay" must be SHORT standalone phrases for overlaying on a thumbnail image -- never a full sentence, never identical to any title above.
- Do not use Markdown code fences.
- Do not include hashtags inside "description" unless the content_type is shorts or you're adding the final hashtag line described above.

Script:

{{ script }}
